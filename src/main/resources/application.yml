#数据库连接信息
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/logs_static_result?serverTimezone=UTC&characterEncoding=utf-8
    password: root
    username: root

#配置mybatis信息

mybatis:
  type-aliases-package: cn.edu.neu.domain
  mapper-locations: classpath:mapper/*.xml
